# Домашнее задание к занятию "Резервное копирование баз данных" - Муравский Артем


---

### Задание 1

Кейс

Финансовая компания решила увеличить надёжность работы баз данных и их резервного копирования.

Необходимо описать, какие варианты резервного копирования подходят в случаях:

1.1. Необходимо восстанавливать данные в полном объёме за предыдущий день.

1.2. Необходимо восстанавливать данные за час до предполагаемой поломки.

1.3.* Возможен ли кейс, когда при поломке базы происходило моментальное переключение на работающую или починенную базу данных.

Приведите ответ в свободной форме.

---

1.1 Нужно восстановить данные в полном объёме за предыдущий день

Самое простое решение: ежедневный полный бэкап (Full Backup)
- Каждый день (например, ночью) делаем **полную копию** всей базы.
- Кладём бэкап **в отдельное хранилище** (другой сервер/объектное хранилище).
- Держим ретеншн, например 7–30 дней.
- Периодически проверяем, что бэкап реально восстанавливается (тестовое восстановление).

**Как восстанавливаем:**
- Берём **вчерашний** полный бэкап.
- Разворачиваем базу из него целиком.

**Что даёт:**
- Максимально просто в понимании и эксплуатации.
- Восстановление “на вчера” делается одной операцией.

---

1.2 Нужно восстановить данные за час до предполагаемой поломки

Самое простое решение: Full Backup + журнал изменений (PITR)
Это называется восстановление “до точки во времени” (Point-in-Time Recovery).

**Как настроить:**
1) Делать **полный бэкап** раз в сутки (как в 1.1).  
2) Включить сохранение **журнала транзакций**:
   - MySQL: binlog
   - PostgreSQL: WAL
   - MS SQL: transaction log  
3) Складывать эти журналы в отдельное хранилище.

**Как восстанавливаем “за час до поломки”:**
- Разворачиваем последний полный бэкап.
- Потом “докатываем” журналы транзакций до времени **T-1 час** (например, 14:00, если авария была около 15:00).

**Что даёт:**
- Можно восстановиться максимально близко к нужному времени (не только “на вчера”).
- Требование “за час до поломки” закрывается напрямую.

---

1.3 Возможен ли кейс мгновенного переключения на работающую/починенную БД

### Да, возможен: репликация + автоматический failover
Самая простая схема: **Primary + Standby replica**.

**Как это работает:**
- Есть основная база (**Primary**, RW).
- Есть реплика (**Standby**, обычно RO), которая постоянно получает изменения.
- При падении Primary система переключает приложение на Standby.

**Что важно понимать:**
- Если репликация **асинхронная**: переключение быстрое, но возможна потеря последних секунд/минут данных.
- Если репликация **синхронная**: потерь почти нет, но запись может быть медленнее (потому что подтверждение ждём от реплики).

---

### Задание 2

2.1. С помощью официальной документации приведите пример команды резервирования данных и восстановления БД (pgdump/pgrestore).

2.2.* Возможно ли автоматизировать этот процесс? Если да, то как?

Приведите ответ в свободной форме.

---

2.1 Пример команд резервного копирования и восстановления (pg_dump / pg_restore)

Ниже — минимальный “рабочий” пример на базе официальной документации PostgreSQL.

Вариант 1 (рекомендуемый для pg_restore): дамп в custom-формате

`pg_restore` работает с архивами, созданными `pg_dump` в **не текстовых форматах** (например, `-Fc` custom или `-Fd` directory). :contentReference[oaicite:0]{index=0}

Резервная копия (backup)

```bash
# Создать дамп базы mydb в custom-формате
pg_dump -Fc -f /backups/mydb_$(date +%F).dump -U myuser -h 127.0.0.1 mydb
```

Восстановление (restore)

Самый простой путь — восстановить в заранее созданную базу:

1) создать пустую базу
`createdb -U myuser -h 127.0.0.1 mydb_restore`

2) восстановить из дампа
`pg_restore -d mydb_restore -U myuser -h 127.0.0.1 /backups/mydb_2025-12-29.dump`


2.2 Можно ли автоматизировать? Да.

1) Cron

Пишем короткий скрипт backup.sh

Ставим расписание в crontab

Пример скрипта:

```bash
#!/usr/bin/env bash
set -euo pipefail

BACKUP_DIR="/backups"
DB="mydb"
USER="myuser"
HOST="127.0.0.1"
FILE="${BACKUP_DIR}/${DB}_$(date +%F_%H-%M).dump"

mkdir -p "$BACKUP_DIR"
pg_dump -Fc -f "$FILE" -U "$USER" -h "$HOST" "$DB"

# простая ротация: удалить файлы старше 14 дней
find "$BACKUP_DIR" -name "${DB}_*.dump" -mtime +14 -delete
```

Cron-задача (каждый день в 02:00):

```bash
0 2 * * * /opt/backup/backup.sh >> /var/log/pg_backup.log 2>&1
```

2) systemd timer

Таймер запускает тот же скрипт. Преимущества: удобнее логирование и управление сервисом

3) Kubernetes CronJob

Если база в Kubernetes/облаке — делаете CronJob, который запускает контейнер с pg_dump и кладёт файл в S3/аналог.


Минимальные практики для автоматизации:

- Хранить бэкапы не на том же диске, где база (иначе авария диска убьёт и БД, и бэкап).

- Делать проверку восстановления (хотя бы раз в неделю на тестовом стенде).

- Вести ротацию (сколько дней хранить) и лог выполнения.

---

### Задание 3

3.1. С помощью официальной документации приведите пример команды инкрементного резервного копирования базы данных MySQL.

3.2.* В каких случаях использование реплики будет давать преимущество по сравнению с обычным резервным копированием?

Приведите ответ в свободной форме.

---

# 3.1 Пример инкрементного бэкапа MySQL (официальная документация)

В MySQL “инкрементный бэкап” в самом простом виде делают через **бинарные логи (binlog)**: полный снимок (например, `mysqldump`) + последующие изменения из binlog. MySQL прямо описывает этот подход как “Point-in-Time (Incremental) Recovery” и приводит пример с `mysqlbinlog`. :contentReference[oaicite:0]{index=0}

**Инкремент “через binlog” командой mysqlbinlog**

Инкрементное резервирование (копирование binlog “накапливающихся изменений”)
Пример “живого” резервирования binlog из официальной документации:

```bash
mysqlbinlog --read-from-remote-server --host=host_name --raw --stop-never binlog.000999
```

Эта команда подключается к серверу и непрерывно сохраняет бинарные логи (то есть изменения) — это и есть инкрементальная часть.

Простейший сценарий восстановления

Восстановить “снимок” (полный дамп), например из mysqldump:

```bash
mysql --host=host_name -u root -p < dump_file
```

“Докатить” изменения из binlog после нужной позиции:

```bash
mysqlbinlog --start-position=27284 binlog.001002 binlog.001003 binlog.001004 | mysql --host=host_name -u root -p
```
Пример с --start-position и “прокатом” нескольких binlog также приведён в официальной документации.

---

3.2 Реплика лучше, когда нужен минимальный простой (быстро пережить падение сервера).
Если основной сервер “умер”, реплика может принять нагрузку быстрее, чем вы поднимете восстановление из бэкапа (у бэкапа обычно выше RTO).

Но важно запомнить!
**Реплика не заменяет бэкап**: если в основной базе случайно удалили данные или записали “плохие” изменения, это обычно уедет и на реплику. Поэтому “обычные бэкапы” всё равно нужны для отката и долгого хранения.